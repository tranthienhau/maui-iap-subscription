<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:SubscriptionPro.Views"
       x:Class="SubscriptionPro.AppShell"
       FlyoutBehavior="Disabled"
       Shell.FlyoutBehavior="Disabled">

	<ShellContent Route="PaywallPage" ContentTemplate="{DataTemplate views:PaywallPage}" />
	<ShellContent Route="SuccessPage" ContentTemplate="{DataTemplate views:SuccessPage}" />
	<ShellContent Route="ManagePage" ContentTemplate="{DataTemplate views:ManagePage}" />

</Shell>
